{
  "name": "@guardian/types",
  "version": "0.5.2",
  "description": "A place for types",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "dist": "npm run build && npm run dist:option && npm run dist:result",
    "dist:option": "cp -r dist/option .",
    "dist:result": "cp -r dist/result .",
    "prepare": "npm run dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/guardian/types.git"
  },
  "author": "",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/guardian/types/issues"
  },
  "homepage": "https://github.com/guardian/types#readme",
  "dependencies": {
    "typescript": "^3.8.3"
  },
  "devDependencies": {
    "@types/jest": "^26.0.8",
    "jest": "^26.2.2",
    "ts-jest": "^26.1.4"
  },
  "jest": {
    "testPathIgnorePatterns": [
      "dist"
    ],
    "preset": "ts-jest"
  }
}
